{
  "name": "prato-feito-backend",
  "module": "src/server.ts",
  "description": "API do PratoFeito, responsável por autenticação, publicações, comentários, reputação, votos e curadoria comunitária de conteúdos gastronômicos.",
  "packageManager": "bun@1.3.10",
  "private": true,
  "type": "module",
  "scripts": {
    "ci": "bun install --frozen-lockfile && bun run build",
    "build": "bunx tsc --noEmit && bun build src/server.ts --target=bun --outdir=dist",
    "start": "NODE_ENV=production bun dist/server.js",
    "dev": "NODE_ENV=development bun docker:up && bun docker:wait:dev && bun migrations:run && bun --watch src/server.ts",
    "test": "NODE_ENV=test bun docker:up && bun docker:wait:test && bun migrations:run && bun test --max-concurrency=1",
    "docker:up": "docker compose -f infra/compose.yaml up -d",
    "docker:down": "docker compose -f infra/compose.yaml down -v",
    "docker:wait:dev": "NODE_ENV=development bun infra/scripts/database-waiting.ts",
    "docker:wait:test": "NODE_ENV=test bun infra/scripts/database-waiting.ts",
    "biome:check": "bunx biome check ./src ./tests ./infra --error-on-warnings",
    "biome:write": "bunx biome check ./src ./tests ./infra --write --unsafe",
    "migrations:create": "node-pg-migrate -m infra/migrations create",
    "migrations:run:test": "node-pg-migrate -m infra/migrations --env-path .env.test up",
    "migrations:run:dev": "node-pg-migrate -m infra/migrations --env-path .env.development up",
    "prepare": "husky"
  },
  "dependencies": {
    "@fastify/cors": "11.3.0",
    "@fastify/swagger": "9.8.1",
    "@fastify/swagger-ui": "6.1.1",
    "dotenv": "17.4.2",
    "dotenv-expand": "13.0.0",
    "fastify": "5.10.0",
    "fastify-type-provider-zod": "7.0.0",
    "pg": "8.22.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@commitlint/cli": "21.2.1",
    "@commitlint/config-conventional": "21.2.0",
    "@types/bun": "1.3.14",
    "@types/node": "24",
    "@types/pg": "8.20.0",
    "husky": "9.1.7",
    "node-pg-migrate": "8.0.4",
    "typescript": "7.0.2"
  }
}
